{
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "gunicorn dict.wsgi --bind 0.0.0.0:$PORT --workers=2 --timeout=120",
      
      
      "restartPolicyType": "NEVER"
    }
  }